const DEFAULT_BUILD_VALUE = 2; // Amount of buildticks units build adjacent buildings by
const MAX_HAND_SIZE = 10;
const MAX_NAME_SIZE = 50;
const TURN_LENGTH = 90 * 1000; // In milliseconds

export {DEFAULT_BUILD_VALUE, MAX_HAND_SIZE, MAX_NAME_SIZE, TURN_LENGTH};
